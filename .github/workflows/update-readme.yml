name: 🔄 Update GitHub Profile README

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Update Random Song
        run: |
          songs=("https://youtu.be/dQw4w9WgXcQ"
                 "https://youtu.be/C0DPdy98e4c"
                 "https://youtu.be/oygrmJFKYZY"
                 "https://youtu.be/2vjPBrBU-TM"
                 "https://youtu.be/kXYiU_JCYtU"
                 "https://youtu.be/fLexgOxsZu0"
                 "https://youtu.be/09R8_2nJtjg")
          random_song=${songs[$RANDOM % ${#songs[@]}]}
          sed -i '/<!-- RANDOM_SONG -->/!b;n;c\🎧 **Now Playing:** '"$random_song" README.md

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "🎧 Auto update random music" || echo "No changes to commit"
          git push
